<UserControl x:Class="SistemaRH.Desktop.Views.RelatorioComparativoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Background="{DynamicResource SurfaceBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0" Text="Relatório Comparativo: CLT vs PJ" FontSize="24" FontWeight="Bold" Margin="20,20,20,15" Foreground="{DynamicResource AccentBlueForeground}"/>

        <!-- Filtros -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="20,0,20,15">
            <StackPanel Width="100" Margin="0,0,10,0">
                <TextBlock Text="Mês:" FontSize="12" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,5"/>
                <ComboBox SelectedItem="{Binding MesSelecionado}" Padding="8" Height="40">
                    <ComboBoxItem>1</ComboBoxItem>
                    <ComboBoxItem>2</ComboBoxItem>
                    <ComboBoxItem>3</ComboBoxItem>
                    <ComboBoxItem>4</ComboBoxItem>
                    <ComboBoxItem>5</ComboBoxItem>
                    <ComboBoxItem>6</ComboBoxItem>
                    <ComboBoxItem>7</ComboBoxItem>
                    <ComboBoxItem>8</ComboBoxItem>
                    <ComboBoxItem>9</ComboBoxItem>
                    <ComboBoxItem>10</ComboBoxItem>
                    <ComboBoxItem>11</ComboBoxItem>
                    <ComboBoxItem>12</ComboBoxItem>
                </ComboBox>
            </StackPanel>

            <StackPanel Width="100" Margin="0,0,10,0">
                <TextBlock Text="Ano:" FontSize="12" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding AnoSelecionado}" Padding="8" Height="40"/>
            </StackPanel>

            <Button Content="📊 Gerar Relatório" Padding="10,5" Margin="0,25,10,0"
                    Background="{DynamicResource BtnNavBackground}" Foreground="{DynamicResource BtnForeground}"
                    Command="{Binding GerarRelatorioCommand}"/>
            <Button Content="📥 Exportar Excel" Padding="10,5" Margin="0,25,0,0"
                    Background="{DynamicResource BtnSuccessBackground}" Foreground="{DynamicResource BtnForeground}"
                    Command="{Binding ExportarExcelCommand}"/>
        </StackPanel>

        <!-- Cards Resumo -->
        <Grid Grid.Row="2" Margin="20,0,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- CLT Count -->
            <Border Grid.Column="0" Background="{DynamicResource AccentBlueBackground}" BorderBrush="{DynamicResource AccentBlueBorder}" BorderThickness="1" CornerRadius="5" Padding="15" Margin="0,0,5,0">
                <StackPanel>
                    <TextBlock Text="Funcionários CLT" FontSize="12" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding RelatorioAtual.QuantidadeCLT}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource AccentBlueForeground}"/>
                </StackPanel>
            </Border>

            <!-- CLT Cost -->
            <Border Grid.Column="1" Background="{DynamicResource AccentGreenBackground}" BorderBrush="{DynamicResource AccentGreenBorder}" BorderThickness="1" CornerRadius="5" Padding="15" Margin="5,0,5,0">
                <StackPanel>
                    <TextBlock Text="Custo CLT (c/ encargos)" FontSize="12" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding RelatorioAtual.CustosCLT, StringFormat='R$ {0:F2}'}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource AccentGreenForeground}"/>
                </StackPanel>
            </Border>

            <!-- PJ Count -->
            <Border Grid.Column="2" Background="{DynamicResource AccentPinkBackground}" BorderBrush="{DynamicResource AccentPinkBorder}" BorderThickness="1" CornerRadius="5" Padding="15" Margin="5,0,5,0">
                <StackPanel>
                    <TextBlock Text="Funcionários PJ" FontSize="12" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding RelatorioAtual.QuantidadePJ}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource AccentPinkForeground}"/>
                </StackPanel>
            </Border>

            <!-- PJ Cost -->
            <Border Grid.Column="3" Background="{DynamicResource AccentYellowBackground}" BorderBrush="{DynamicResource AccentYellowBorder}" BorderThickness="1" CornerRadius="5" Padding="15" Margin="5,0,0,0">
                <StackPanel>
                    <TextBlock Text="Custo Total PJ" FontSize="12" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding RelatorioAtual.CustosPJ, StringFormat='R$ {0:F2}'}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource AccentYellowForeground}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Análise -->
        <Grid Grid.Row="3" Margin="20,0,20,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Diferença -->
            <Border Grid.Column="0" Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource BorderCard}" BorderThickness="1" CornerRadius="5" Padding="15" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Análise Comparativa" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource AccentBlueForeground}" Margin="0,0,0,15"/>

                    <StackPanel>
                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="Custo Total (CLT + PJ)" FontSize="12" Foreground="{DynamicResource TextMuted}"/>
                            <TextBlock Text="{Binding RelatorioAtual.CustoTotal, StringFormat='R$ {0:F2}'}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="Diferença Absoluta" FontSize="12" Foreground="{DynamicResource TextMuted}"/>
                            <TextBlock Text="{Binding RelatorioAtual.DiferencaAbsoluta, StringFormat='R$ {0:F2}'}" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource ValueRed}"/>
                        </StackPanel>

                        <StackPanel>
                            <TextBlock Text="Percentual PJ" FontSize="12" Foreground="{DynamicResource TextMuted}"/>
                            <TextBlock Text="{Binding RelatorioAtual.PercentualPJ, StringFormat='{}{0:F1}%'}" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource AccentBlueForeground}"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Custos Médios -->
            <Border Grid.Column="1" Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource BorderCard}" BorderThickness="1" CornerRadius="5" Padding="15" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="Custo Médio por Funcionário" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource AccentBlueForeground}" Margin="0,0,0,15"/>

                    <StackPanel>
                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="Custo Médio CLT" FontSize="12" Foreground="{DynamicResource TextMuted}"/>
                            <TextBlock Text="{Binding RelatorioAtual.CustoMedioCLT, StringFormat='R$ {0:F2}'}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource ValueGreen}"/>
                        </StackPanel>

                        <StackPanel>
                            <TextBlock Text="Custo Médio PJ" FontSize="12" Foreground="{DynamicResource TextMuted}"/>
                            <TextBlock Text="{Binding RelatorioAtual.CustoMedioPJ, StringFormat='R$ {0:F2}'}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource ValuePink}"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Status -->
        <Border Grid.Row="4" Background="{DynamicResource StatusBarBackground}" BorderBrush="{DynamicResource BorderStatusBar}" BorderThickness="0,1,0,0" Padding="10,10,10,5" Margin="20,15,20,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource TextMuted}" FontSize="12"/>
        </Border>
    </Grid>
</UserControl>
