<Window x:Class="SistemaRH.Desktop.Views.UsuariosView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Usuários" Height="500" Width="620" WindowStartupLocation="CenterOwner" Loaded="Window_Loaded">
    <Grid Background="{DynamicResource SurfaceBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource HeaderBackground}" Padding="15">
            <TextBlock Text="Usuários do Sistema" Foreground="{DynamicResource HeaderForeground}" FontSize="18" FontWeight="Bold"/>
        </Border>

        <Border Grid.Row="1" Background="{DynamicResource ActionBarBackground}" Padding="15,10" BorderBrush="{DynamicResource BorderActionBar}" BorderThickness="0,0,0,1">
            <Button Content="+ Novo Usuário" Command="{Binding NovoCommand}"
                    Background="{DynamicResource BtnPrimaryBackground}" Foreground="{DynamicResource BtnForeground}" Padding="15,8"
                    FontWeight="Bold" Cursor="Hand" HorizontalAlignment="Left"/>
        </Border>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Usuarios}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  SelectionMode="Single" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" RowHeight="44"
                  Background="{DynamicResource DataGridBackground}"
                  AlternatingRowBackground="{DynamicResource AlternatingRowBackground}"
                  BorderThickness="0" Margin="15,10">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Nome de Usuário" Binding="{Binding NomeUsuario}" Width="*" MinWidth="140"/>
                <DataGridTemplateColumn Header="Status" Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="Ativo" Foreground="{DynamicResource AccentGreenForeground}" FontWeight="SemiBold" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Ativo}" Value="False">
                                                <Setter Property="Text" Value="Inativo"/>
                                                <Setter Property="Foreground" Value="{DynamicResource TextMuted}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Criado em" Binding="{Binding DataCriacao, StringFormat=dd/MM/yyyy}" Width="110"/>
                <DataGridTemplateColumn Header="Ações" Width="220">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Content="Editar" Padding="8,4" Margin="0,0,5,0"
                                        Background="{DynamicResource BtnNavBackground}" Foreground="{DynamicResource BtnForeground}" FontSize="11"
                                        Command="{Binding DataContext.EditarCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Cursor="Hand"/>
                                <Button Content="Desativar" Padding="8,4" FontSize="11" Cursor="Hand"
                                        Background="{DynamicResource BtnDangerBackground}" Foreground="{DynamicResource BtnForeground}"
                                        Command="{Binding DataContext.DesativarCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Visibility="{Binding Ativo, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <Button Content="Ativar" Padding="8,4" FontSize="11" Cursor="Hand"
                                        Background="{DynamicResource BtnSuccessBackground}" Foreground="{DynamicResource BtnForeground}"
                                        Command="{Binding DataContext.AtivarCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Visibility="{Binding Ativo, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" Padding="15">
            <Button Content="Fechar" Padding="15,8" Background="{DynamicResource BtnNavBackground}" Foreground="{DynamicResource BtnForeground}" Width="100" HorizontalAlignment="Right" Click="Fechar_Click"/>
        </Border>
    </Grid>
</Window>
