<Window x:Class="SistemaRH.Desktop.Views.CampoPersonalizadoDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="{Binding Titulo}" Height="600" Width="440" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Loaded="Window_Loaded">
    <Grid Background="{DynamicResource SurfaceBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource HeaderBackground}" Padding="15">
            <TextBlock Text="{Binding Titulo}" Foreground="{DynamicResource HeaderForeground}" FontSize="18" FontWeight="Bold"/>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="Rótulo *" FontSize="12" Foreground="{DynamicResource TextLabel}" Margin="0,0,0,5"/>
            <TextBox Padding="10" BorderBrush="{DynamicResource BorderDefault}" BorderThickness="1" Height="40" Text="{Binding Rotulo, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Tipo de campo *" FontSize="12" Foreground="{DynamicResource TextLabel}" Margin="0,15,0,5"/>
            <ComboBox Height="40" Padding="10,0" VerticalContentAlignment="Center"
                      SelectedValuePath="Tag" SelectedValue="{Binding TipoTexto}">
                <ComboBoxItem Content="Texto" Tag="Texto"/>
                <ComboBoxItem Content="Número" Tag="Numero"/>
                <ComboBoxItem Content="Monetário" Tag="Monetario"/>
                <ComboBoxItem Content="Data" Tag="Data"/>
                <ComboBoxItem Content="Checkbox (sim/não)" Tag="Checkbox"/>
                <ComboBoxItem Content="Lista de opções" Tag="Lista"/>
            </ComboBox>

            <StackPanel Visibility="{Binding MostrarOpcoes, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock Text="Opções (separadas por vírgula) *" FontSize="12" Foreground="{DynamicResource TextLabel}" Margin="0,15,0,5"/>
                <TextBox Padding="10" BorderBrush="{DynamicResource BorderDefault}" BorderThickness="1" Height="40"
                         Text="{Binding Opcoes, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Ex.: Solteiro, Casado, Divorciado"/>
            </StackPanel>

            <TextBlock Text="Aplicável a" FontSize="12" Foreground="{DynamicResource TextLabel}" Margin="0,15,0,5"/>
            <ComboBox Height="40" Padding="10,0" VerticalContentAlignment="Center"
                      SelectedValuePath="Tag" SelectedValue="{Binding AplicavelATexto}">
                <ComboBoxItem Content="Todos os tipos" Tag="Todos"/>
                <ComboBoxItem Content="Somente CLT" Tag="CLT"/>
                <ComboBoxItem Content="Somente PJ" Tag="PJ"/>
                <ComboBoxItem Content="Somente Estagiário" Tag="Estagiario"/>
            </ComboBox>

            <TextBlock Text="Ordem de exibição" FontSize="12" Foreground="{DynamicResource TextLabel}" Margin="0,15,0,5"/>
            <TextBox Padding="10" BorderBrush="{DynamicResource BorderDefault}" BorderThickness="1" Height="40" Text="{Binding Ordem, UpdateSourceTrigger=PropertyChanged}"/>

            <CheckBox Content="Obrigatório" IsChecked="{Binding Obrigatorio}" Margin="0,18,0,0" Foreground="{DynamicResource TextLabel}"/>
            <CheckBox Content="Ativo" IsChecked="{Binding Ativo}" Margin="0,10,0,0" Foreground="{DynamicResource TextLabel}"/>
        </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="2" Padding="20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancelar" Padding="15,8" Background="{DynamicResource BtnDangerBackground}" Foreground="{DynamicResource BtnForeground}" Width="100" Margin="0,0,10,0" Command="{Binding CancelarCommand}"/>
                <Button Content="Salvar" Padding="15,8" Background="{DynamicResource BtnSuccessBackground}" Foreground="{DynamicResource BtnForeground}" Width="100" Command="{Binding SalvarCommand}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
